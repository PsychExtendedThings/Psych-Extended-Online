name: Build

on:
  workflow_call:
    inputs:
      name:
        required: true
        type: string
      os:
        required: true
        type: string
      setupCommand:
        required: true
        type: string
      buildArgs:
        required: true
        type: string
      artifactName:
        required: true
        type: string
      artifactPath:
        required: true
        type: string
      doCache:
        required: true
        type: string

jobs:
  build:
    name: ${{ inputs.name }}
    runs-on: ${{ inputs.os }}
    permissions:
      contents: write # Needed for the release to work
    steps:
      - name: Checkout
        uses: actions/checkout@main

      - name: Setup Haxe
        uses: krdlab/setup-haxe@master
        with:
          # This is the "Magic Fix" for the Unexpected ? error
          haxe-version: 4.3.4 

      - name: Install Libraries
        run: |
          haxelib setup ~/haxelib
          ${{ inputs.setupCommand }}
          
          # This fixes the "lime setup android" error from your previous log
          if [ "${{ runner.os }}" = "Linux" ]; then
            haxelib run lime config ANDROID_SDK $ANDROID_HOME
            haxelib run lime config ANDROID_NDK $ANDROID_NDK_LATEST_HOME
            haxelib run lime setup android -noprompt
          fi
        shell: bash

      - name: Compile
        run: haxelib run lime build ${{ inputs.buildArgs }}
        shell: bash

      - name: Upload Artifact
        uses: actions/upload-artifact@main
        with:
          name: ${{ inputs.artifactName }}
          path: ${{ inputs.artifactPath }}

      # ADDED: Upload Release (using softprops/shopify-style action)
      - name: Upload Release
        uses: softprops/action-gh-release@v1
        if: success()
        with:
          tag_name: "v${{ github.run_number }}"
          name: "Psych Online Build #${{ github.run_number }}"
          body: "Automated Build for Psych Extended Online"
          files: ${{ inputs.artifactPath }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
